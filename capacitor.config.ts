import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mgo1-frontend',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
